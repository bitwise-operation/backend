source 'https://rubygems.org'

gem 'bundler'
gem 'rake'

gem 'lotusrb',      '0.2.1'
gem 'lotus-model', '~> 0.2'

gem 'activemodel'
gem 'active_model_serializers'

group :test do
  gem 'minitest'
  gem 'capybara'
end

group :development, :test do
  gem 'pry'
end

group :production do
  # gem 'puma'
end
