ruby '2.2.1'

source 'https://rubygems.org'

gem 'bundler'
gem 'rake'

gem 'lotusrb',      '0.2.1'
gem 'lotus-model', '~> 0.2'

gem 'activesupport'

gem 'pg'

group :test do
  gem 'minitest'
  gem 'capybara'
  gem 'database_cleaner'
end

group :development, :test do
  gem 'pry'
end

group :production do
  # gem 'puma'
end
